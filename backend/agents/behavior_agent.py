def get_behavior_profile(user_id: int) -> dict:
    # Placeholder profile until behavior analytics history is connected.
    return {
        "risk_profile": "moderate",
        "adherence_score": 0.4,
        "preferred_holding_years": 10,
    }